<?xml version="1.0" encoding="UTF-8" ?>
        <!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Config 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.mapper.read.UserKeyRead">
    <select id="getUserKey" resultType="com.domain.UserKey" parameterType="string">
        SELECT * from userkey where openid=#{openId}
    </select>
</mapper>